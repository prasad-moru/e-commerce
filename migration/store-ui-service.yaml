apiVersion: v1
kind: Service
metadata:
  name: store-ui-service
  namespace: e-commerce
spec:
  type: NodePort
  ports:
  - name: http
    port: 80
    targetPort: 80
    nodePort: 32080
  selector:
    app: store-ui-deployment
