apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: shared-services
spec:
  type: NodePort
  ports:
  - protocol: TCP
    port: 6379
    targetPort: 6379
    nodePort: 32379
  selector:
    app: redis-deployment
